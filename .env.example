# OKX dev portal creds for the Market API (Phase 0-A)
OKX_API_KEY=
OKX_SECRET_KEY=
OKX_PASSPHRASE=

# Venice child key with spend cap (Phase 0-D)
VENICE_API_KEY=

ANTHROPIC_API_KEY=

# Product economics — price never moves
PRICE_USDT=1
READ_BUDGET_USD=0.30

# X Layer USDT payout address (Phase 4)
PAYOUT_ADDRESS=

DATABASE_PATH=./data/optic.db
CARDS_DIR=./data/cards
PUBLIC_BASE_URL=http://localhost:3000
CACHE_TTL_SECONDS=600

# Phase 1: false (stub passes through). Phase 4: true (real x402 flow)
PAYMENTS_ENFORCED=false

PORT=3000
TICKET_PROOF_PK=        # Polymarket proof wallet key (never commit real value)
